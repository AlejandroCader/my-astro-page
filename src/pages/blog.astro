---
import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/BlogPost.astro";
const pageTitle = "Mi blog de Astro aprendizaje";
const allPosts = await Astro.glob("../pages/posts/*.md");
---

<BaseLayout pageTitle={pageTitle}>
  <h1>Este es mi blog!</h1>
  <p>
    Estoy trabajando en el tutorial introductorio de Astro. Esta es la tercera
    página de mi sitio web, ¡y es la primera que he construido yo mismo!
  </p>
  <p>Aquí es donde publicaré sobre mi viaje de aprendizaje Astro.</p>
  <ul>
    {
      allPosts.map((post) => (
        <BlogPost url={post.url} title={post.frontmatter.title} />
      ))
    }
  </ul>
</BaseLayout>
